// Wolfie Landing Page - Next.js + Tailwind CSS + Framer Motion

// 1. إنشاء مشروع جديد Next.js
// npx create-next-app wolfie-landing

// 2. تثبيت Tailwind CSS
// npm install -D tailwindcss postcss autoprefixer
// npx tailwindcss init -p

// tailwind.config.js
module.exports = {
  content: ['./pages/**/*.{js,ts,jsx,tsx}', './components/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
}

// globals.css
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  @apply bg-gradient-to-r from-purple-400 via-pink-500 to-red-500;
}

// 3. صفحة index.js
import { motion } from 'framer-motion';

export default function Home() {
  const features = [
    { title: 'عمولة منخفضة', desc: '10–15% فقط مقابل 20–30% في UberEats/DoorDash', icon: '💰' },
    { title: 'أجر تنافسي للسائقين', desc: 'دفع أفضل يحفز الالتزام ويقلل الدوران الوظيفي', icon: '🏍️' },
    { title: 'تجربة مستخدم رائعة', desc: 'واجهة عصرية Glassmorphism وتجربة سلسة', icon: '✨' },
    { title: 'شفافية كاملة', desc: 'تقييم ومتابعة الطلبات بشكل مباشر', icon: '🔍' },
    { title: 'توصيل أسرع', desc: 'نظام توجيه ذكي يقلل التأخير', icon: '⚡' },
  ];

  const restaurants = [
    { name: 'مطعم بيتزا نيويورك', feature: 'توصيل سريع', img: '/restaurant1.jpg' },
    { name: 'مطعم برجر ستار', feature: 'تقييم عالي', img: '/restaurant2.jpg' },
    { name: 'مطعم سوشي سيتي', feature: 'أطباق مبتكرة', img: '/restaurant3.jpg' },
  ];

  return (
    <div className="min-h-screen flex flex-col items-center justify-center p-8">
      {/* Hero Section */}
      <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 1 }} className="w-full max-w-6xl p-8 bg-white/20 backdrop-blur-md rounded-3xl mb-12">
        <h1 className="text-5xl font-bold text-white mb-4">Deliver Smarter with Wolfie</h1>
        <p className="text-white mb-6">منصة ذكية تربط المطاعم بالسائقين في NYC بسرعة وأمان</p>
        <button className="px-8 py-4 bg-black text-white rounded-full hover:scale-105 transition transform">Be Wolfie</button>
      </motion.div>

      {/* Features Section */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
        {features.map((f, i) => (
          <motion.div key={i} whileHover={{ scale: 1.05 }} className="p-6 bg-white/20 backdrop-blur-md rounded-2xl shadow-lg flex flex-col items-center text-center">
            <span className="text-4xl mb-4">{f.icon}</span>
            <h2 className="text-2xl font-semibold mb-2 text-white">{f.title}</h2>
            <p className="text-white/90">{f.desc}</p>
          </motion.div>
        ))}
      </div>

      {/* Restaurants/SDrivers Section */}
      <div className="w-full max-w-6xl grid grid-cols-1 md:grid-cols-3 gap-6">
        {restaurants.map((r, i) => (
          <motion.div key={i} whileHover={{ scale: 1.05 }} className="bg-white/20 backdrop-blur-md rounded-2xl shadow-lg p-4 flex flex-col items-center">
            <img src={r.img} alt={r.name} className="w-32 h-32 object-cover rounded-full mb-4" />
            <h3 className="text-xl font-bold text-white mb-2">{r.name}</h3>
            <p className="text-white/90 mb-2">{r.feature}</p>
            <button className="px-4 py-2 bg-black text-white rounded-full hover:scale-105 transition transform">اختيار</button>
          </motion.div>
        ))}
      </div>
    </div>
  );
}
